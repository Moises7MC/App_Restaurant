name: app_restaurant
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # Estado y arquitectura
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  
  # Navegación
  go_router: ^14.0.0
  
  # UI
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  
  # Utilidades
  shared_preferences: ^2.2.2
  uuid: ^4.0.0
  intl: ^0.19.0
  http: ^1.1.0
  signalr_netcore: ^1.3.5  # ← AGREGA ESTA LÍNEA

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
  
  # Aquí irán las imágenes más adelante
  # assets:
  #   - assets/images/